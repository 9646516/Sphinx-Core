#include <bits/stdc++.h>
using namespace std;
int main(int argc, char *argv[]) {
    // assert(freopen("/data/1.out", "r", stdin));
    string a;
    while (cin >> a) {
        cout << a << endl;
    }
    return 0;
}
